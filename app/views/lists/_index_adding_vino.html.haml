%table.table
  %thead
    %tr
      %th(width="30%") Pulse la lista a la que añadir el vino
  %tbody
    - @lists.each do |list|
      %tr
        %td
          = button_to "#{ list.nombre }", list_path(list) 
        %td
          = button_to "#{ list.user.full_name }", user_path(list.user)